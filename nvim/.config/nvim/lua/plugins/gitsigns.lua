return {
    -- gitsigns provides gutter symbols for additions, modifications, removals, etc. in git
    "lewis6991/gitsigns.nvim",
    -- load after reading a buffer into memory
    event = "BufRead",
}
